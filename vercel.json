{
  "version": 3,
  "framework": "vite",
  "buildCommand": "pnpm run build:vercel",
  "devCommand": "pnpm run dev",
  "outputDirectory": "dist/public",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
